/**
 * Order domain layer: aggregates, entities, value objects, domain events and
 * repository interfaces (ports) for the Order bounded context. No framework
 * dependencies belong here.
 */
package com.ticketdd.order.domain;
