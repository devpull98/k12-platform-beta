/**
 * Booking domain layer (ticket inventory + seat/ticket hold): aggregates,
 * entities, value objects, domain events and repository interfaces (ports)
 * for the Ticket/Booking bounded context. No framework dependencies belong
 * here.
 */
package com.ticketdd.booking.domain;
