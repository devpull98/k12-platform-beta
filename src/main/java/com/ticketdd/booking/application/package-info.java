/**
 * Booking application layer: use-case services/command handlers (e.g. hold
 * seat, confirm booking) that orchestrate domain objects and repository
 * ports. No web or persistence framework types belong here.
 */
package com.ticketdd.booking.application;
